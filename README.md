# HomeCenter

CREATE DATABASE `HOME_CENTER_DB`;
CREATE USER 'HOME_CENTER_USER'@'localhost' IDENTIFIED BY 'HOME_CENTER_PASSWORD';
GRANT ALL PRIVILEGES ON `HOME_CENTER_DB`.* TO 'HOME_CENTER_USER'@'localhost';
FLUSH PRIVILEGES;